version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly
  allow-parallel-runners: true

linters:
  enable:
    - gosec
    - errcheck
    - staticcheck
    - govet
    - revive
    - gocyclo
    - misspell
    - unparam
    - unused
    - prealloc
    - bodyclose

linters-settings:
  revive:
    rules:
      exported:
        severity: warning
      package-comments:
        severity: warning
  gosec:
    excludes:
      - G104
  gocyclo:
    min-complexity: 15

issues:
  max-issues-per-linter: 0
  max-same-issues: 0